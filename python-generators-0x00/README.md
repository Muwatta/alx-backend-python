# Python Generators – Task 0

This project focuses on using Python generators to process data efficiently.

Task 0 sets up the MySQL database `ALX_prodev`, creates the `user_data` table, and seeds it using data from `user_data.csv`.
